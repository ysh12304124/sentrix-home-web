# 交接说明

本目录是从 `/mnt/d/company/NOTE/WorldMM-a` 的关键帧流水线源码整理出的独立交接包。

压缩包不包含：

- 视频或其他数据集；
- YOLO、YOLO-World、表情模型等权重；
- `.venv`、缓存、日志和运行输出。

第三方 Python 依赖按各自项目许可使用。使用前请确认同事机器上的 CUDA、PyTorch 和模型权重符合其部署环境。
